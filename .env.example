# SQLite file (default: ./data/portfolio.db)
DATABASE_URL=file:./data/portfolio.db

# Admin login at /admin/login (optional — app defaults apply if unset in dev)
ADMIN_USERNAME=madhusudantimalsina
ADMIN_PASSWORD=2925398627864619

# Session cookie signing — required in production (16+ chars). Dev uses a built-in fallback if unset.
ADMIN_SESSION_SECRET=replace-with-long-random-string-at-least-16-chars

# Quick start: npm run seed:admin  (creates .env.local once if missing)

# Contact form (existing)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
